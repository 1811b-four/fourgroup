

server:
  port: 8768


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


spring:
  application:
    name: springcloud-provider
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jmh?serverTimezone=Hongkong&&characterEncoding=utf8&useSSL=false
    username: root
    password: 201220

mybatis:
  mapper-locations: mapper/*.xml